"""Per-method Quick Settings widgets for ``AnalysisSettingsSidebar`` - one
file per ``AnalysisKind``, per the "one concern per file" principle from the
Analysis Case refactor's approved plan. Only Time History has real content
so far; the other four kinds still use a plain placeholder page directly in
``analysis_settings_sidebar.py`` until their own turn comes.
"""
